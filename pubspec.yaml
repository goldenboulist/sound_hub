name: sound_hub
description: A Windows soundboard application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite_common_ffi: ^2.3.2
  path_provider: ^2.1.2
  audioplayers: ^6.0.0
  file_picker: ^10.3.10
  shared_preferences: ^2.2.2
  uuid: ^4.4.0
  path: ^1.9.0
  provider: ^6.1.2
  desktop_drop: ^0.7.0
  flutter_audio_output: ^0.0.4
  flutter_svg: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
